# =============================================================================
# StoryZop — Dependencies
# =============================================================================
# Local development dependencies (safe to install without GPU)
# =============================================================================

# Browser automation
playwright>=1.50

# Image processing
Pillow>=10.0

# Data validation
pydantic>=2.0
pydantic-settings>=2.0

# Environment / secrets
python-dotenv>=1.0

# Testing
pytest>=8.0
pytest-asyncio>=0.24

# Async support for notebooks
nest-asyncio>=1.6

# =============================================================================
# Colab-only dependencies (install in notebook, NOT locally)
# =============================================================================
# torch>=2.1
# torchvision>=0.16
# transformers>=4.57.0
# accelerate>=0.30
# qwen-vl-utils
# bitsandbytes>=0.43
# easyocr>=1.7
# flash-attn  (optional, for FlashAttention-2 on supported GPUs)
